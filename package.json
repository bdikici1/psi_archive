{
  "name": "psi_archive",
  "version": "1.0.0",
  "private": true,
  "description": "ψ-Glif sistemi, GLYPH_TOKEN ve NFT altyapısı",
  "scripts": {
    "test": "echo \"No tests yet\"",
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy_token.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
